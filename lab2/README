DataImpl - Хранилище предопределенных токенов способов их обработки
AnalyzerImpl - Анализатор входящего выражения, проверяет его корректность(если в выражении допущена ошибка выбрасывает исключение) и формирует список готовый к вычислению
Calc - Получает на вход проанализированный список, переводит его в польскую нотацию, вычисляет выражение и возвращает результат в Double
MyCalculator - Собирает три предыдущих класса воедино: принимает на вход выражение(обращаясь в DataImpl за функциями для вычисления) и возвращает результат в Double
